# Railway 部署配置
# 文档：https://docs.railway.app/deploy/config-as-code

[deploy]
# 启动命令
startCommand = "sh -c 'envsubst \"\\$PORT\" < /app/nginx.conf > /tmp/nginx.conf && nginx -c /tmp/nginx.conf -g \"daemon off;\"'"

# 健康检查
healthcheckPath = "/"
healthcheckTimeout = 30

[deploy.retry]
max = 3
